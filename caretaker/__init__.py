"""caretaker — per-GPU-host manager daemon owning the llama-server lifecycle.

This is the bootstrap skeleton (PLAN.md §1). Lifecycle logic lands in later
phases (A–E); today the package ships an empty, walkable layout.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]